// Configuration for the Frontend
// For local development, leave this empty ("") to use relative paths.
// For production (Firebase + Cloud Run), set this to your Cloud Run Service URL.
// Example: const API_BASE_URL = "https://halo-viz-backend-xyz.run.app";
const API_BASE_URL = "";
