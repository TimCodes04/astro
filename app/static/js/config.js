// Configuration for the Frontend
// For local development, leave this empty ("") to use relative paths.
// Production (Cloud Run with Firebase Rewrites):
// Since Firebase proxies requests to Cloud Run, we use relative paths.
const API_BASE_URL = "";
